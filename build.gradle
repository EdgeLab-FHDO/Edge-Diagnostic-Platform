plugins {
    //Java plugin (No main method to run)
    id 'java'
}
group 'de.fh-dortmund'
version '0.5'

repositories {
    //Where to get the dependencies
    mavenCentral()
}

dependencies {
    testImplementation 'junit:junit:4.12'
}
