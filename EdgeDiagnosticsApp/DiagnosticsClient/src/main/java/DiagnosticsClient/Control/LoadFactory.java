package DiagnosticsClient.Control;

import DiagnosticsClient.Load.LoadTypes.DiagnosticsLoad;
import DiagnosticsClient.Load.LoadTypes.FileLoad;
import DiagnosticsClient.Load.LoadTypes.PingLoad;

public class LoadFactory {

    /*public DiagnosticsLoad createLoad(Instruction instruction) {
        int rep = instruction.getRepetitions();
        int interval = instruction.getInterval();
        return switch (instruction.getLoadType()){
            case PING -> new PingLoad(,rep,interval);
            case FILE -> new FileLoad()
        }
    }*/
}
